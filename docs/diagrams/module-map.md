# ARKSpace — Module Map
# أرك سبيس — خريطة الوحدات

```mermaid
graph LR
    subgraph ARKSpace
        Core["ARKSpace Core"]
        Settings["AS Settings"]
    end
    style Core fill:#1B365D,color:#fff
```

> **Note:** This is a placeholder module map. Update with actual modules from `modules.txt`.
